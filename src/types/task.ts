// Re-export types from tasks components for backward compatibility
export type {
  Task,
  PerformerPerson,
  TaskStatus,
  TaskOperation,
  FilterOptions,
  PaginationInfo,
} from '@/components/tasks/types';
